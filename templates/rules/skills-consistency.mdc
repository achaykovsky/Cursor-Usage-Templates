---
description: Skills consistency - structure, terminology, routing boundaries
globs: ".cursor/skills/**/SKILL.md, templates/skills/**/SKILL.md"
alwaysApply: false
---
# Skills Consistency Standards

**Structure**: Use concise sections with stable ordering: `Workflow`, `Output Contract` (when applicable), `Notes` and/or `Routing` guidance.

**Terminology**: Use canonical severity terms (`CRITICAL`, `WARNING`, `GOOD`) when severity is needed.

**Boundaries**: Prefer explicit routing lines when skill overlap exists (e.g., FE-local vs cross-stack/system-wide).

**Contracts over defaults**: For API behavior, prefer explicit project contract/policy over framework defaults.

**Constraint formatting**: Do not chain independent requirements with semicolons. Use bullets or separate lines so agents can parse each requirement independently.

**Semicolon exception**: Semicolons are acceptable only for tightly related sub-actions inside a single workflow step.

**Canonical references**: Link named rules, skills, and catalogs to canonical files/indexes when available (`rules/*.mdc`, `skills/SKILLS.md`, section entry `README.md`).

**Authoring path**: Edit `templates/skills/**/SKILL.md`; sync (or `sync-templates-to-local` hook) copies to `.cursor/skills/`.

**No contradictory examples**: Do not include snippets that violate security/rule guidance (e.g., hardcoded secrets, unsafe defaults).
